# Настройка CI/CD для проекта

## 🚀 Что настроено

### Автоматические проверки

- **ESLint** - проверка качества кода
- **Prettier** - проверка форматирования кода
- **TypeScript** - проверка типов
- **Vitest** - запуск тестов

### Пайплайн по веткам

#### 🌿 Ветка `main`

При push или pull request в main:

- ✅ Запускаются все проверки (линтеры, тесты, typecheck)
- ❌ Деплой НЕ происходит

#### 🚀 Ветка `prod`

При push в prod:

- ✅ Запускаются все проверки (линтеры, тесты, typecheck)
- ✅ Происходит сборка проекта (`npm run build`)
- ✅ Автоматический деплой на хостинг

## 📋 Локальная разработка

### Команды для проверки перед коммитом:

```bash
# Проверка линтера
npm run lint

# Автоисправление проблем линтера
npm run lint:fix

# Проверка форматирования
npm run format:check

# Автоформатирование
npm run format

# Проверка типов
npm run typecheck

# Запуск тестов
npm run test:run

# Запуск тестов в watch режиме
npm run test

# Запуск тестов с UI
npm run test:ui
```

### Все проверки одной командой:

```bash
npm run lint && npm run format:check && npm run typecheck && npm run test:run
```

## 🔧 Настройка деплоя

### 1. Создайте ветку prod

```bash
git checkout -b prod
git push origin prod
```

### 2. Настройте GitHub Secrets

Перейдите в Settings > Secrets and variables > Actions и добавьте:

**Для FTP деплоя:**

- `FTP_SERVER` - адрес вашего FTP сервера
- `FTP_USERNAME` - имя пользователя FTP
- `FTP_PASSWORD` - пароль FTP

**Для SSH деплоя:**

- `HOST_IP` - IP адрес сервера
- `HOST_USERNAME` - имя пользователя на сервере
- `HOST_SSH_KEY` - приватный SSH ключ

### 3. Выберите способ деплоя

Отредактируйте файл `.github/workflows/deploy.yml` и раскомментируйте нужный способ деплоя в секции "Deploy to hosting".

Подробные примеры смотрите в файле `.github/workflows/deploy-examples.md`.

## 🔄 Workflow

### Обычная разработка:

1. Создайте feature ветку от `main`
2. Разрабатывайте и тестируйте локально
3. Создайте Pull Request в `main`
4. GitHub автоматически проверит код
5. После мерджа код попадает в `main`

### Релиз в продакшн:

1. Создайте Pull Request из `main` в `prod`
2. После мерджа в `prod` произойдет автоматический деплой

## 🐛 Troubleshooting

### Тесты не проходят

```bash
# Запустите тесты локально
npm run test:run

# Проверьте конфигурацию
cat vitest.config.ts
```

### Линтер ругается

```bash
# Автоисправление
npm run lint:fix

# Проверка конкретного файла
npx eslint components/Button.vue
```

### Проблемы с типами

```bash
# Проверка типов
npm run typecheck

# Генерация типов Nuxt
npm run postinstall
```

## 📊 Мониторинг

Проверить статус пайплайна можно в GitHub:

- Перейдите в Actions вкладку
- Выберите нужный workflow
- Посмотрите логи выполнения

## 🎯 Следующие шаги

1. Настройте GitHub Secrets для деплоя
2. Протестируйте пайплайн на ветке `main`
3. Создайте ветку `prod` и протестируйте деплой
4. Добавьте больше тестов для критически важных компонентов
